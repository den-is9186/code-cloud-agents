echo "🧪 Running pre-push checks..."

# Tests
echo "🧪 Running tests..."
npm test || {
  echo "⚠️  Tests failed, but allowing push to continue"
  echo "ℹ️  Please fix tests before merging to main/develop"
  exit 0
}

echo "✅ Pre-push checks passed!"
